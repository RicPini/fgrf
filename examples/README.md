# Examples

This folder contains minimal examples for running FGRF v1 and v2 on different models.

## Requirements

```bash
pip install transformers accelerate bitsandbytes datasets pandas scikit-learn psutil scipy
